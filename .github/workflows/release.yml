name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
          - os: macos-14
            target: x86_64-apple-darwin
            archive: tar.gz
          - os: macos-14
            target: aarch64-apple-darwin
            archive: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip

    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: release-${{ matrix.target }}
      - run: cargo build --release --locked --bin sentinel --target ${{ matrix.target }}
      - name: Package binary
        shell: pwsh
        run: |
          $target = "${{ matrix.target }}"
          $version = "${{ github.ref_name }}"
          $package = "sentinel-$version-$target"
          $root = Join-Path "dist" $package
          New-Item -ItemType Directory -Force -Path $root | Out-Null

          $binary = if ($IsWindows) { "sentinel.exe" } else { "sentinel" }
          Copy-Item "target/$target/release/$binary" "$root/$binary"
          Copy-Item "README.md" "$root/README.md"
          Copy-Item "LICENSE" "$root/LICENSE"

          if ("${{ matrix.archive }}" -eq "zip") {
            $asset = "dist/$package.zip"
            Compress-Archive -Path $root -DestinationPath $asset -Force
          } else {
            $asset = "dist/$package.tar.gz"
            tar -C dist -czf $asset $package
          }

          $hash = (Get-FileHash $asset -Algorithm SHA256).Hash.ToLowerInvariant()
          $assetName = Split-Path $asset -Leaf
          "$hash  $assetName" | Out-File "$asset.sha256" -Encoding ascii -NoNewline
      - uses: actions/upload-artifact@v7
        with:
          name: sentinel-${{ matrix.target }}
          path: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.sha256
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true
      - uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.sha256

  smoke-install-unix:
    needs: publish
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-14
    steps:
      - uses: actions/checkout@v6
      - name: Install published release archive
        shell: bash
        env:
          SENTINEL_VERSION: ${{ github.ref_name }}
          SENTINEL_INSTALL_DIR: ${{ runner.temp }}/sentinel-bin
        run: |
          sh scripts/install.sh
          "$SENTINEL_INSTALL_DIR/sentinel" --version
          "$SENTINEL_INSTALL_DIR/sentinel" rules validate rules

  smoke-install-windows:
    needs: publish
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install published release archive
        shell: pwsh
        env:
          SENTINEL_VERSION: ${{ github.ref_name }}
          SENTINEL_INSTALL_DIR: ${{ runner.temp }}\sentinel-bin
        run: |
          ./scripts/install.ps1
          & "$env:SENTINEL_INSTALL_DIR\sentinel.exe" --version
          & "$env:SENTINEL_INSTALL_DIR\sentinel.exe" rules validate rules
